# 01_생성형 AI 및 LLM

---

## 1. 생성형 AI vs LLM 개념 및 차이점

두 용어는 자주 함께 혼용되지만, **포함 관계(범위)**에서 차이가 있습니다.

[ 인공지능 (AI) ]
└── [ 머신러닝 (ML) ]
└── [ 딥러닝 (Deep Learning) ]
└── [ 생성형 AI (Generative AI) ]
└── [ LLM (Large Language Model) ]  ← 텍스트 중심 특화 영역

| 구분 | 생성형 AI (Generative AI) | LLM (Large Language Model) |
| :--- | :--- | :--- |
| **정의** | 데이터의 패턴을 학습해 새로운 콘텐츠를 만들어내는 AI 기술 전체 | 방대한 텍스트 데이터를 학습하여 자연어를 이해하고 생성하는 대규모 언어 모델 |
| **출력 모달리티** | 텍스트, 이미지, 음성, 비디오, 3D 등 | 주로 **텍스트**, 코드, 수식 |
| **대표 예시** | ChatGPT, Midjourney, Sora, Suno | GPT-4o, Claude 3.5, Gemini, LLaMA 3 |

---

## 2. 생성형 AI의 핵심 기술 및 트랜스포머 (Transformer)

생성형 AI와 LLM의 핵심 모태는 **트랜스포머(Transformer)** 아키텍처입니다.

### 핵심 매커니즘: 셀프 어텐션 (Self-Attention)
* **개념:** 문장 내의 모든 단어가 서로 어떤 관계를 맺고 있는지 맥락(Context)을 파악하는 알고리즘.
* **작동 방식:** 입력된 단어들 간의 연관성 점수를 계산하여, 문맥상 가장 중요한 단어에 중점(Attention)을 둡니다.

### 모달리티별 주요 AI 모델
1. **텍스트 (LLM):** Transformer 디코더 기반 (예: GPT 계열)
2. **이미지/영상:** 
   * **Diffusion Model:** 이미지에 노이즈를 섞은 후, 역으로 노이즈를 제거해가며 고화질 이미지를 생성하는 모델 (Midjourney, Stable Diffusion).
   * **GAN (생성적 적대 신경망):** 생성자(Generator)와 판별자(Discriminator)가 경쟁하며 진짜 같은 데이터를 만듦.

---

## 3. LLM은 어떻게 학습되는가? (3단계 학습 과정)

LLM이 사용자의 질문을 이해하고 안전하게 답변하기까지는 아래 3단계를 거칩니다.

[ 1단계: 사전 학습 ] ➔ [ 2단계: 지도 미세조정 ] ➔ [ 3단계: RLHF ]
(Pre-training)          (SFT)               (강화학습 정렬)


1. **사전 학습 (Pre-training):**
   * 웹상의 대규모 텍스트 데이터를 이용해 "다음 단어 예측"을 학습.
   * 모델에게 기본적인 언어 지식과 세상에 대한 지식을 주입하는 단계 (엄청난 컴퓨팅 자원 필요).
2. **지도 미세조정 (SFT, Supervised Fine-Tuning):**
   * "질문-답변" 형태의 정제된 데이터셋으로 모델을 추가 학습시켜 지시를 수행하도록 교정.
3. **인간 피드백 기반 강화학습 (RLHF, Reinforcement Learning from Human Feedback):**
   * 인간이 답변의 선호도(품질, 안전성)를 평가하여 보상 모델을 만들고, 이를 기반으로 유해하거나 편향된 답변을 피하도록 유도.

---

## 4. LLM의 주요 한계와 보완 기술

LLM을 실제 현업이나 서비스에 적용할 때 가장 자주 발생하는 문제와 이를 보완하는 기술입니다.

* **환각 현상 (Hallucination):** 모델이 그럴듯하지만 전혀 사실이 아닌 내용을 지어내는 현상.
* **최신성 부재:** 사전 학습 시점 이후의 정적 지식에 머물러 있음.
* **보완 기술 (RAG & Agent):**
  * **RAG (검색 증강 생성):** 외부 DB나 최신 검색 정보를 실시간으로 조회하여 이를 기반으로 답변을 작성하도록 유도.
  * **AI Agent (에이전트):** 단순 텍스트 생성을 넘어 API 호출, 코드 실행, 계산기 사용 등 외부 도구를 활용해 복잡한 문제 해결.
  * **SLM (소규모 언어 모델):** 성능은 유지하면서 모델 크기를 경량화하여 오프라인 온디바이스(On-device) 환경이나 비용 절감용으로 활용.

---

### 💡 학습 점검 퀴즈
1. **Q. 생성형 AI와 LLM 중 더 넓은 개념은 무엇일까요?**
   * *(정답: 생성형 AI. LLM은 텍스트 생성을 전담하는 생성형 AI의 하위 영역입니다.)*
2. **Q. LLM이 사실이 아닌 내용을 진짜처럼 거짓으로 생성하는 현상을 무엇이라 부를까요?**
   * *(정답: 환각 현상 / Hallucination)*
