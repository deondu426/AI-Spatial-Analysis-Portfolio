# 📋 서울특별시 노원구 재난 대피소 최적 입지 선정 실습 프로젝트 종합 보고서

## 1. 프로젝트 개요
* **프로젝트명:** 서울특별시 노원구 재난 대피소 최적 입지 선정 (GIS-MCDA)
* **분석 대상 및 단위:** 서울특별시 노원구 전체 영역 / 100m × 100m 표준 격자 (총 4,087개, EPSG:5179)
* **평가 지표 및 가중치:** **인구수 (40%) + 대피소 소외도 (30%) + 도로 접근성 (30%)**

---

## 2. 전체 세부 진행 단계 및 트러블슈팅

### [1단계] 대상지 공간 데이터 로드 및 노원구 영역 추출 (Filtering)
* **수행 내용:** 
  1. 서울특별시 전체 행정구역 공간 데이터(`seoul_admin.shp`) 및 국토조사 100m 표준 격자 레이어 로드.
  2. 속성 테이블 표현식을 통한 선택(`"SIG_KOR_NM" = '노원구'`)으로 노원구 행정구역 폴리곤 분리.
  3. **공간 자르기 (Clip):** 추출된 노원구 경계로 100m 격자망을 자르고, 투영 좌표계를 `EPSG:5179`(UTM-K)로 통일하여 노원구 전용 격자 레이어(4,087개 피처) 구축.
* **이유 및 목적:** 서울시 전체 데이터 연산 시 발생하는 불필요한 시스템 메모리 소모를 줄이고, 정확한 거리 및 면적 연산을 위해 보정 좌표계 기반의 노원구 단일 연구 영역을 확정함.

---

### [2단계] 입지 평가지표 관련 데이터 로드 및 공간 거리 연산
* **수행 내용:**
  1. **대피소 데이터 결합:** 기존 대피 시설 위치(Point) 레이어 로드 후, 격자 중심점과의 최소 거리 연산(`distance` 필드 생성).
  2. **도로망 데이터 결합:** 도로망(Line) 레이어 로드 후, 각 격자 중심점부터 가장 가까운 도로선까지의 최소 거리 연산(`road_dist` 필드 생성).
  3. **인구 데이터 결합:** SGIS 100m 인구 격자 데이터를 위치 기반 공간 결합(Spatial Join)하여 `pop_field_4` 필드로 병합.
* **이유 및 목적:** MCDA 모델 구동에 필요한 3가지 핵심 축(인구 밀집도, 기존 대피소 소외도, 도로 접근성)의 원천 속성 데이터를 공간 연산을 통해 격자 단위로 모으기 위함.

---

### [3단계] 데이터 전처리 & 결측치(NULL) `0` 치환
* **수행 내용:** `road_dist`(도로 거리) 및 `pop_field_4`(인구 수) 필드 내 존재하는 NULL 값을 `0`으로 치환함.
* **발생 이슈 및 원인:** 
  * SGIS 인구 격자 데이터에는 수락산·불암산 같은 산림 지역, 중랑천 수변 구역, 도로 등 사람이 살지 않는 비거주 구역에 인구 수치가 없어 DB 상에 값이 비어있는 `NULL` 상태로 수집됨.
* **해결 방법:** 
  * GIS 필드 계산기는 연산 대상에 `NULL`이 하나라도 포함되어 있으면 전체 수식 결과값을 `NULL`(Class Error)로 처리함. 따라서 "사람이 살지 않는 구역 = 인구수 0명"이라는 실제 공간 현상(True Zero)에 맞춰 `0`으로 일괄 정제하여 공간 연산이 단절되지 않도록 함.

---

### [4단계] `n_pop` 정규화 연산 오류 해결 (`pop_field_4` 텍스트 형변환)
* **수행 내용:** 인구수 수치를 0~1 사이로 정규화하는 `n_pop` 필드 생성.
* **발생 이슈 및 원인:** 
  * `n_pop` 정규화 결과가 0~1 사이가 아니라 **3.1515...** 와 같이 상한선(1.0)을 크게 초과하는 이상치 에러 발생.
  * 원천 데이터의 `pop_field_4` 필드가 숫자가 아닌 **텍스트(String) 타입**이었기 때문임. 문자열 정렬에서는 숫자의 수치 크기가 아니라 아스키코드 순서로 비교하므로 `312`보다 `"99"`(첫 글자 9)를 더 큰 값으로 인식하여 최댓값(`maximum`)이 `99`로 오계산되면서 $\frac{312}{99} = 3.1515$ 오류가 난 것임.
* **해결 방법:** 
  * 잘못 계산된 `n_pop` 및 `mcda_score` 필드를 속성 테이블에서 삭제(`Ctrl + L`).
  * 수식 내에 `to_real("pop_field_4")` 함수를 사용하여 텍스트를 실수(Real) 타입으로 강제 형변환(Type Casting)한 뒤 정규화 재연산 수행.

---

### [5단계] 지표별 Min-Max 정규화 및 변수 방향성 통일
* **수행 내용:** 서로 단위(명, m)가 다른 3개 지표를 0~1 스케일로 맞추고 방향성을 통일함.
* **세부 연산 수식:**
  1. **인구 점수 (`n_pop`):** 인구가 많을수록 대피 수혜 인원이 많아 대피소가 시급함 (편익 지표)
     $$\text{n\_pop} = \frac{\text{to\_real("pop\_field\_4")} - \text{minimum}}{\text{maximum} - \text{minimum}}$$
  2. **대피소 소외 점수 (`n_shelter`):** 기존 대피소와 거리가 멀수록 방재 사각지대임 (편익 지표)
     $$\text{n\_shelter} = \frac{\text{distance} - \text{minimum}}{\text{maximum} - \text{minimum}}$$
  3. **도로 접근 점수 (`n_road`):** 도로 거리가 짧을수록(가까울수록) 접근성이 뛰어남 (비용 지표)
     * 거리가 가까울수록 1에 수렴하도록 **역산(Inversion) 수식** 적용:
     $$\text{n\_road} = \frac{\text{maximum} - \text{road\_dist}}{\text{maximum} - \text{minimum}}$$

---

### [6단계] 가중 선형 합산(WLC)을 통한 종합 적합도(`mcda_score`) 산출
* **수행 내용:** 정규화된 3개 필드에 가중치 비중을 적용하여 가중 합산 연산 수행.
  ```text
  ("n_pop" * 0.4) + ("n_shelter" * 0.3) + ("n_road" * 0.3)
 * **발생 이슈 및 원인:** 
  * "제공자에 새 필드를 추가할 수 없습니다"라는 에러 팝업 발생.
  * 이미 `mcda_score` 필드가 정상 생성 완료되었는데, 처리 과정에서 [확인] 버튼을 연속 클릭하면서 필드가 중복 생성되어 QGIS 엔진이 차단한 것임.
* **해결 방법:** 
  * 에러 팝업을 닫고 필드 계산기 창을 취소로 종료한 후, 속성 테이블 맨 우측에 `mcda_score` 값(0.497... 등)이 정상 산출된 것을 확인하고 편집 모드 저장(`Ctrl + E`).

---

### [7단계] 단계 구분(Graduated) 심볼화 및 지도 시각화
* **수행 내용:** `mcda_score` 점수에 따라 노원구 4,087개 격자 색상을 5단계로 구분하여 시각화.
* **발생 이슈 및 원인:** 
  * 초기 설정 시 지도 전체가 분류되지 않고 단색(주황색)으로만 통일되어 표출됨.
  * 심볼 분류 모드가 '고정 간격(1.0)'으로 지정되어 있어, 0~1 사이 소수점 값인 `mcda_score`가 단일 구간으로 처리되었기 때문임.
* **해결 방법:** 
  * 모드를 **`내추럴 브레이크 (Jenks)`**로 변경하여 데이터 내부의 자연스러운 단절 구간을 기준으로 5단계 재분류.
  * 격자마다 적용된 검은색 테두리 선이 지도를 어둡게 가리는 시각 노이즈를 해결하기 위해 심볼 설정에서 **`획(stroke) 스타일`을 `획 없음`으로 변경**하여 열지도(Heatmap) 형태의 깔끔한 시각화 완성.

---

## 3. 최종 시각화 결과 공간 해석
* **북부 외곽 구역 (상계동 외곽 및 수락산 인근):** 기존 대피 시설과의 물리적 거리가 멀어 대피소 소외도 점수(`n_shelter`)가 극대화된 방재 사각지대로, 가장 진한 붉은색 영역으로 표출됨.
* **중·남부 주거 밀집 구역 (아파트 단지 및 역세권 주변):** 높은 인구 밀도로 인해 인구수 가중치(40%)가 높게 반영되어, 신규 대피소 설치 시 최대 수혜를 볼 수 있는 주요 입지들로 강조됨.
  
