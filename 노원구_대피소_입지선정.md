# 📋 서울특별시 노원구 재난 대피소 최적 입지 선정 실습 프로젝트 보고서

## 1. 프로젝트 개요
* **프로젝트명:** 서울특별시 노원구 재난 대피소 최적 입지 선정 (GIS-MCDA)
* **분석 단위:** 노원구 100m × 100m 격자 (총 4,087개, EPSG:5179)
* **평가 지표 및 가중치:** **인구수 (40%) + 대피소 소외도 (30%) + 도로 접근성 (30%)**

---

## 2. 세부 진행 단계 및 이슈 트러블슈팅

### [1단계] 데이터 전처리 & 결측치(NULL) `0` 치환
* **수행 내용:** `road_dist`(도로 거리) 및 인구 필드(`pop_field_4`) 내 존재하는 NULL 값을 `0`으로 치환함.
* **발생 이슈 및 원인:** 
  * SGIS 인구 격자 데이터에는 수락산·불암산 같은 산림 지역, 중랑천 수변, 도로 등 사람이 살지 않는 비거주 구역에 인구 수치가 없어 DB 상에 값이 비어있는 `NULL` 상태로 포함됨.
* **해결 방법:** 
  * GIS 필드 계산기는 연산 대상에 `NULL`이 하나라도 포함되어 있으면 전체 결과값을 `NULL`(오류)로 처리함. 따라서 "사람이 살지 않는 구역 = 인구수 0명"이라는 실제 상황에 맞춰 `0`으로 일괄 정제하여 연산이 끊기지 않도록 처리함.

---

### [2단계] `n_pop` 정규화 연산 오류 해결 (`pop_field_4` 텍스트 형변환)
* **수행 내용:** 인구수를 0~1 사이로 정규화하는 `n_pop` 필드 생성.
* **발생 이슈 및 원인:** 
  * `n_pop` 결과가 0~1 사이가 아니라 **3.1515...** 로 1을 넘는 오류 발생.
  * 원천 데이터의 `pop_field_4` 필드가 숫자가 아닌 **텍스트(String) 타입**이었기 때문임. 문자열 정렬에서는 숫자의 크기가 아니라 문자 순서로 비교하므로 `312`보다 `"99"`(9로 시작)를 더 큰 값으로 인식하여 최댓값(`maximum`)이 `99`로 잡히면서 $\frac{312}{99} = 3.1515$ 오류가 발생함.
* **해결 방법:** 
  * 잘못 계산된 `n_pop`과 `mcda_score` 필드를 속성 테이블에서 삭제(`Ctrl + L`).
  * 수식 내에 `to_real("pop_field_4")` 함수를 사용하여 텍스트를 실수(Real)로 강제 형변환한 뒤 정규화 재계산.

---

### [3단계] 지표별 Min-Max 정규화 및 변수 방향성 통일
* **수행 내용:** 서로 단위(명, m)가 다른 3개 지표를 0~1 범위로 정규화함.
* **세부 연산 논리:**
  1. **인구 점수 (`n_pop`):** 인구가 많을수록 대피소 필요성이 높음 (편익)
     $$\text{n\_pop} = \frac{\text{to\_real("pop\_field\_4")} - \text{minimum}}{\text{maximum} - \text{minimum}}$$
  2. **대피소 소외 점수 (`n_shelter`):** 기존 대피소와 거리가 멀수록 대피소가 시급함 (편익)
     $$\text{n\_shelter} = \frac{\text{distance} - \text{minimum}}{\text{maximum} - \text{minimum}}$$
  3. **도로 접근 점수 (`n_road`):** 도로 거리가 짧을수록(가까울수록) 접근성이 좋음 (비용)
     * 거리가 가까울수록 1에 수렴하도록 **역산(Inversion) 수식** 적용:
     $$\text{n\_road} = \frac{\text{maximum} - \text{road\_dist}}{\text{maximum} - \text{minimum}}$$

---

### [4단계] 최종 적합도 점수 (`mcda_score`) 산출
* **수행 내용:** 정규화된 3개 필드에 가중치를 곱해 합산.
  ```text
  ("n_pop" * 0.4) + ("n_shelter" * 0.3) + ("n_road" * 0.3)
* **발생 이슈 및 원인:** 
  * "제공자에 새 필드를 추가할 수 없습니다"라는 에러 팝업 발생.
  * 상단 안내창을 확인해 보니 이미 `mcda_score` 필드가 정상 생성 완료되었는데, [확인] 버튼을 연속으로 클릭하면서 필드가 중복 생성되어 QGIS가 차단한 것임.
* **해결 방법:** 
  * 에러 팝업을 닫고 필드 계산기 창을 취소로 닫은 후, 속성 테이블 맨 우측에 `mcda_score` 값(0.497... 등)이 정상적으로 들어온 것을 확인하고 편집 모드 저장(`Ctrl + E`).

---

### [5단계] 단계 구분(Graduated) 심볼화 및 지도 시각화
* **수행 내용:** `mcda_score` 점수에 따라 노원구 격자 색상을 5단계로 시각화.
* **발생 이슈 및 원인:** 
  * 처음에 심볼 분류가 안 되고 단색(주황색)으로 표시됨.
  * 분류 모드가 '고정 간격(1.0)'으로 되어 있어 소수점 점수들이 분류되지 않았던 것임.
* **해결 방법:** 
  * 모드를 **`내추럴 브레이크 (Jenks)`**로 변경하여 데이터의 자연스러운 단절 구간을 기준으로 5단계 재분류.
  * 4,087개 격자의 검은색 테두리가 지도를 어둡게 가리는 문제를 해결하기 위해 심볼 설정에서 **`획(stroke) 스타일`을 `획 없음`으로 변경**하여 열지도(Heatmap) 형태로 시각화 완성.

---

## 3. 최종 시각화 결과 공간 해석
* **북부 외곽 구역:** 기존 대피소와 거리가 멀어 대피소 소외도 점수(`n_shelter`)가 높은 방재 사각지대로 붉게 표출됨.
* **중·남부 주거 밀집 구역:** 인구수 가중치(40%)가 높게 반영되어, 대피소 신설 시 수혜 인원이 많은 주거지 격자들이 높은 점수대로 강조됨.
